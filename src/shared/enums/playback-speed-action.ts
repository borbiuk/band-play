/**
 * Enumeration of playback speed actions.
 * Used to control track playback speed adjustments.
 */
export enum PlaybackSpeedAction {
	/** Reset playback speed to normal (1.0x) */
	Reset = 1,
	/** Increase playback speed */
	Increase = 2,
	/** Decrease playback speed */
	Decrease = 3,
}
