/**
 * Enumeration of playback pitch actions.
 * Used to control auto-pitch preservation functionality.
 */
export enum PlaybackPitchAction {
	/** Reset auto-pitch settings to default */
	Reset = 1,
	/** Toggle auto-pitch preservation on/off */
	Switch = 2,
}
