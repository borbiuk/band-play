---
description: Create a .zip build artifact for publishing (runs ./zip.sh)
allowed-tools: Bash(npm run zip)
---

Run `npm run zip` and report the name of the created archive. If dist/ is missing or stale, run `npm run build` first.
