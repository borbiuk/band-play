---
description: Build the Band Play Chrome extension into dist/ (webpack.prod.js)
allowed-tools: Bash(npm run build)
---

Run `npm run build` and report the result. If the build fails, show the errors and propose minimal fixes.
